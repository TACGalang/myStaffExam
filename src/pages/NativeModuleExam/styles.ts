import {StyleSheet} from 'react-native';

const Styles = () =>
  StyleSheet.create({
    container: {
      flex: 1,
    },
    activityIndicator: {
      flex: 1,
    },
    listContainer: {
      flex: 1,
    },
  });

export default Styles;
